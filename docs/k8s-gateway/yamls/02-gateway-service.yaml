# 网关 Service
# 注意：selector 必须与 Pod 的 labels 完全匹配
# 坑点：kubectl expose 会自动添加 run: 标签导致不匹配
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  selector:
    app: gateway
  ports:
  - port: 80
    targetPort: 80